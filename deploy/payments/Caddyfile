{
	admin off
}

{$PAYMENT_DOMAIN} {
	request_body {
		max_size 300KB
	}
	# No access log, request body logging or application payload transformations.
	reverse_proxy backend:8790 {
		transport http {
			dial_timeout 5s
			response_header_timeout 120s
		}
	}
}
