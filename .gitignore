# dependencies & build
node_modules/
dist/
release/

# env（本地私有配置，不入库）
.env
.env.local
.env.production
.env.*.local

# editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# misc
*.zip
*.log
.claude/

# 守护进程：本地配置(可能含 Key)与产出数据，不入库
server/explorer.config.json
explorer-vault/

# 登录后端本地启动脚本（含 Resend Key），不入库
server/start-auth.sh

*.pptx
docs/推广/

# Capacitor 原生工程：由 `npx cap add android/ios` 生成
# 生成物很大且平台相关，签名文件更不能进仓库。
# 想让 CI 能构建的话，把 android/ 提交进来（去掉下面这行），
# 但 keystore / google-services.json / GoogleService-Info.plist 永远不要提交。
android/
ios/
*.keystore
*.jks
google-services.json
GoogleService-Info.plist
